= form_tag charges_path do
  %article
    %label.amount
      %span 
        Amount: $15.00
  %script(src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button" data-key="#{@stripe_btn_data[:key]}" data-amount="#{@stripe_btn_data[:amount]}" data-description="#{@stripe_btn_data[:description]}")
